"""Paperhand's Beat Tools — PySide6 desktop app (sub-project A).

The GUI catalogs beats in place, auto-analyzes them, and lets you tag and
audition them. It reuses the shared audio engine in the top-level ``core``
package for all detection/mixing/export so the batch CLI and the GUI never
diverge.
"""
