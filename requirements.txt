# Paperhand's Beat Tools — batch tagger dependencies.
# Install with:  python -m pip install -r requirements.txt
# Also requires ffmpeg on PATH (see README): winget install Gyan.FFmpeg

pydub>=0.25.1      # audio load / overlay / duck / export (uses ffmpeg)
librosa>=0.10.0    # BPM + key + drop detection
soundfile>=0.12.1  # audio decoding backend for librosa
numpy>=1.24        # array math
pandas>=2.0        # CSV report
Pillow>=10.0       # procedural cover-art generation (GUI)

# Optional — stem separation (DAW Mode / Split Stems). Heavy (pulls PyTorch).
# The app offers a one-click install of this when needed:
#   pip install demucs
# (we save stems via soundfile, so torchcodec is NOT required)

# Dev / testing only:
pytest>=8.0
